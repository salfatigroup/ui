{
  "name": "nuxt-app",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev -p 4200",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "pnpm -C ../../packages/ui/ run build; nuxt prepare",
    "clean": "rimraf dist node_modules .turbo .nuxt .output"
  },
  "devDependencies": {
    "@nuxt/devtools": "^0.6.7",
    "@types/node": "^20.4.1",
    "nuxt": "^3.6.2",
    "rimraf": "latest"
  },
  "dependencies": {
    "@nuxtjs/google-fonts": "^3.0.1",
    "@salfatigroup/tailwind-config": "workspace:*",
    "@salfatigroup/ui": "workspace:*",
    "autoprefixer": "^10.4.14",
    "postcss": "^8.4.25",
    "tailwindcss": "^3.3.2"
  }
}
